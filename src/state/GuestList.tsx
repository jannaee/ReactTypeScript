const GuestList: React.FC = () => {
    //If not expecting to recieve props then don't include the <> with an interface ie
    // React.FC<interface>
    return <div>Guest List </div>
};
export default GuestList;